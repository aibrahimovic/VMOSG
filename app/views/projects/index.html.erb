<p id="notice"><%= notice %></p>

<!DOCTYPE html>
<html lang="en">
<head>
  <title>Osnovne informacije</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
</head>
<body>
<br><br>
<div class="container">
  <h2>Informacije o projektima</h2>
<br>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Naziv projekta</th>
      <th>Opis projekta</th>
      <th>Početak realizacije</th>
      <th>Kraj realizacije</th>
      <th>Slika</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.description %></td>
        <td><%= project.start %></td>
        <td><%= project.end %></td>
        <td><%= project.image %></td>
        <td><%= link_to 'Izmjena', edit_project_path(project) %></td>
        <td><%= link_to 'Brisanje', project, method: :delete, data: { confirm: 'Da li ste sigurni?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Kreiraj novi projekat', new_project_path, class: "btn btn-info", style: 'color: white;' %><br><br><br>
<%= link_to 'Nazad na admin stranicu', admin_main_path, class: "btn btn-default" %>
<%= link_to 'Nazad na web stranicu', statics_home_path, class: "btn btn-default" %><br><br>


