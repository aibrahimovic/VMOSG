<p id="notice"><%= notice %></p>

<!DOCTYPE html>
<html lang="en">
<head>
  <title>Osnovne informacije</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
</head>
<body>
<br><br>
<div class="container">
  <h2>Informacije o udruženjima</h2>

<br>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Naziv udruženja</th>
      <th>Opis udruženja</th>
      <th>Web stranica</th>
      <th>Telefon</th>
      <th>Image</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @members.each do |member| %>
      <tr>
        <td><%= member.name %></td>
        <td><%= member.description %></td>
        <td><%= member.web %></td>
        <td><%= member.phone %></td>
        <td><%= member.image %></td>
        <td><%= link_to 'Izmjena', edit_member_path(member) %></td>
        <td><%= link_to 'Brisanje', member, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'Kreiraj novo udruženje', new_member_path, class: "btn btn-info", style: 'color: white;' %><br><br>
<br>
<%= link_to 'Nazad na admin stranicu', admin_main_path, class: "btn btn-default" %>
<%= link_to 'Nazad na web stranicu', statics_home_path, class: "btn btn-default" %><br><br>

